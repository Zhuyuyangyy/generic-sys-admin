# 权利要求书

**发明名称：一种基于自然语言驱动的因果感知企业资源管理方法**

---

## 独立权利要求

### 权利要求1（方法权利要求）

一种自然语言驱动的因果影响预测方法，其特征在于，包括以下步骤：

**S1. 意图解析步骤**：接收用户输入的自然语言指令，利用基于规则的解析器提取操作意图和实体标识；
  - 所述操作意图包括以下类型之一：QUERY、CREATE、UPDATE、DELETE、STATISTICS；
  - 所述实体标识通过正则表达式模式匹配提取，实体类型包括设备标识 EQ-*、耗材标识 CS-* 和巡检记录标识 IN-*；

**S2. 因果传播触发步骤**：当且仅当所述操作意图为 DELETE 或 UPDATE 时，触发因果图传播计算；
  - 所述触发条件由意图识别模块自动判断，无需人工干预；

**S3. 因果图传播步骤**：基于预构建的系统因果有向无环图，以指定节点为源头执行宽度优先搜索遍历；
  - 在所述宽度优先搜索遍历过程中，每跨越一条有向边将当前影响权重乘以衰减系数0.8；
  - 当某下游节点的影响权重低于预设截断阈值0.01时，停止向该节点的后续路径继续传播；
  - 每一节点的影响权重等于该节点到源头节点的路径上各边衰减系数的乘积；

**S4. 影响结果返回步骤**：根据所述宽度优先搜索遍历结果，返回受影响节点的标识集合；
  - 其中每一所述受影响节点关联其对应的当前影响权重值；
  - 若任一所述受影响节点的权重值不低于高影响阈值0.5，则返回高影响标记。

**S5. 执行与报告生成步骤**：基于所述操作意图和实体标识，通过反射机制调用对应的业务服务方法执行实际操作；
  - 将实际操作执行结果与所述受影响节点集合合并，生成包含执行结果、影响范围和高影响标记的因果检查报告。

---

### 权利要求2（系统权利要求）

一种实现权利要求1所述方法的因果感知企业资源管理系统，其特征在于，包括：

**NL解析模块**：配置于 `com.zyy.nl` 包下的 `NLParser.java` 类；
  - 所述NL解析模块内置基于关键词匹配的意图识别规则，关键词集合与操作意图类型的对应关系由配置文件维护；
  - 所述NL解析模块的正则表达式模式至少包括：`EQ-(\\d+)`、`CS-(\\d+)`、`IN-(\\d+)`；

**因果传播引擎模块**：配置于 `com.zyy.causal` 包下的 `CausalPropagationEngine.java` 类；
  - 所述因果传播引擎模块以 `com.zyy.causal.CausalDAG` 类所维护的邻接表为遍历数据结构；
  - 所述因果传播引擎模块在传播过程中维护节点访问标记集合以防止重复遍历；
  - 所述衰减系数0.8和所述截断阈值0.01以类成员变量形式注入所述因果传播引擎模块；

**因果图构建模块**：配置于 `com.zyy.nl` 包下的 `CausalDAGService.java` 类；
  - 所述因果图构建模块在系统初始化阶段通过 `@PostConstruct` 注解触发生成系统级因果有向无环图；
  - 所述系统级因果有向无环图至少包含：设备管理节点、耗材管理节点、操作日志节点和耗材告警节点；
  - 所述系统级因果有向无环图的边类型由 `com.zyy.causal.CausalMechanism` 枚举定义；

**WebSocket推送模块**：配置于 `com.zyy.websocket` 包下的 `WebSocketService.java` 类；
  - 所述WebSocket推送模块在检测到高影响标记时，主动通过STOMP协议向订阅端点推送告警消息；
  - 所述WebSocket配置由 `com.zyy.websocket.WebSocketConfig.java` 类以 `@EnableWebSocketMessageBroker` 注解启用；
  - 推送消息的目标端点前缀为 `/topic`，握手端点为 `/ws`；

**操作日志切面模块**：配置于 `com.zyy.aspect` 包下的 `OperationLogAspect.java` 类；
  - 所述操作日志切面模块在捕获Controller方法执行后，异步调用所述WebSocket推送模块的 `pushOperationLog` 方法；
  - 所述调用以静默方式执行，WebSocket推送失败不阻断业务操作流程。

---

### 权利要求3（计算机可读存储介质权利要求）

一种计算机可读存储介质，其上存储有计算机程序指令，该计算机程序指令被处理器执行时实现权利要求1所述方法的全部步骤。

---

## 从属权利要求

### 权利要求4

根据权利要求1所述的方法，其特征在于：所述因果有向无环图的每条有向边关联一个因果机制类型标签，所述因果机制类型标签包括五种枚举值：

- `FOREIGN_KEY`：外键依赖关系，表示业务实体间的引用约束；
- `ETL_TRANSFORM`：数据转换关系，表示数据在传递过程中的格式或语义转换；
- `SCHEMA_DEPENDENCY`：数据库Schema依赖，表示表结构变更对下游查询的影响；
- `QUALITY_PROPAGATION`：质量传播，表示数据质量指标在传递路径上的逐层演变；
- `CASCADING_FAILURE`：级联故障，表示某节点异常状态沿因果链向下游扩散；

所述因果传播引擎在遍历每条有向边时，根据该边的因果机制类型标签计算该次传播的附加衰减系数，其中：

- `FOREIGN_KEY` 类型的边以基础衰减系数 1.0 通过，表示外键约束导致的精确业务关联，传播过程中不产生额外衰减；
- `QUALITY_PROPAGATION` 类型的边以附加衰减系数 0.9 通过，与基础衰减系数 0.8 叠加后实际衰减为 0.72，表示质量信息传递存在非确定性损耗；
- `CASCADING_FAILURE` 类型的边以附加衰减系数 1.0 通过，但触发阈值设置为高于其他类型的权重阈值 0.3，用于优先触发告警和人工审批流程；
- `ETL_TRANSFORM` 和 `SCHEMA_DEPENDENCY` 类型的边以默认附加衰减系数 1.0 通过。

所述因果机制枚举实现于 `com.zyy.causal.CausalMechanism.java`，每条边构造时以 `com.zyy.causal.CausalEdge(String fromId, String toId, CausalMechanism mechanism)` 构造方法将机制类型与边绑定。

---

### 权利要求5

根据权利要求1所述的方法，其特征在于：
- 在所述宽度优先搜索遍历过程中，使用 `java.util.LinkedList` 作为队列数据结构；
- 所述受影响节点的当前影响权重通过 `java.util.LinkedHashMap` 按访问顺序记录；
- 所述访问顺序保证权重值从源头节点到下游节点递减排列。

---

### 权利要求6

根据权利要求1所述的方法，其特征在于：
- 所述高影响阈值0.5通过 `hasHighImpact()` 方法判定；
- 存在任一所述受影响节点的权重值不低于0.5时，所述高影响标记为真；
- 所述高影响标记用于前端界面的红色警示渲染或人工审批拦截。

---

### 权利要求7

根据权利要求1所述的方法，其特征在于：
- 当所述NL解析模块未能从所述自然语言指令中提取到有效实体标识时，跳过所述因果传播触发步骤；
- 此时直接执行所述执行与报告生成步骤，且返回的受影响节点集合为空集；
- 所述自然语言指令中包含"查询"、"获取"、"列出"关键词时，识别为 QUERY 意图；
- 所述自然语言指令中包含"删除"、"移除"关键词时，识别为 DELETE 意图。

---

### 权利要求8

根据权利要求2所述的系统，其特征在于：
- 所述系统级因果有向无环图在初始化阶段通过 `@PostConstruct` 注解触发生成；
- 所述初始化过程至少创建以下类型的节点：业务实体节点、系统模块节点和系统服务节点；
- 所述业务实体节点与所述系统模块节点之间以 `FOREIGN_KEY` 类型的边连接，表示业务层面的外键约束关系；
- 所述系统模块节点与所述系统服务节点之间以 `QUALITY_PROPAGATION` 或 `SCHEMA_DEPENDENCY` 类型的边连接，表示质量信息传递或日志记录的服务依赖关系；
- 所述系统级因果有向无环图的结构通过 `CausalDAG.addNode(CausalNode)` 和 `CausalDAG.addEdge(String, String, CausalMechanism)` 方法逐步构建。

---

### 权利要求9

根据权利要求1所述的方法，其特征在于：
- 在执行实际操作之前，通过 `CausalCheckResult.summary()` 方法生成人类可读的影响摘要；
- 所述影响摘要格式为：`[因果预测] 影响{N}个节点，最严重: {nodeId} (权重={weight}) | 操作结果: {result}`；
- 若因果传播未触发，所述摘要格式为：`[因果预测未触发] {result}`。

---

### 权利要求10

根据权利要求2所述的系统，其特征在于：
- 所述NL解析模块通过 `NLRuleEngine.resolveService(NLParser.ParseResult)` 方法将操作意图和实体类型映射为对应的业务服务方法名；
- 所述业务服务方法通过 `NLExecutor.dispatch(String serviceMethod, String entityId)` 方法以Java反射机制调用；
- 所述反射调用支持以实体数字标识为参数调用 `EquipmentService`、`ConsumableService` 和 `InspectionService` 的相应方法；
- 当反射方法调用抛出异常时，返回包含异常信息的字符串，不阻断其他业务处理流程。

---

### 权利要求11

根据权利要求1所述的方法，其特征在于：
- 通过 `CausalDAG.topologicalSort()` 方法对所述因果有向无环图执行Kahn拓扑排序；
- 在将任一边加入所述邻接表之前，验证所述边的起点节点和终点节点均已存在于节点集合中；
- 若所述验证失败，所述边的添加操作被静默跳过。

---

## 实施例

以下实施例引用实际Java类路径：

**实施例1 — NL解析与因果传播的完整调用链**

```
用户输入："删除设备 EQ-1"

流程：
1. NLParser.parse("删除设备 EQ-1")
   → NLIntent.DELETE, entityIds=["EQ-1"], entityType="EQUIPMENT"
   （实现：`com.zyy.nl.NLParser.parse(String)`）

2. NLService.executeWithCausalCheck() 检测到 DELETE 意图
   → 调用 CausalDAGService.predictImpact("EQUIPMENT", "EQ-1")
   （实现：`com.zyy.nl.CausalDAGService.predictImpact(String, String)`）

3. CausalDAGService.resolveNodeId() 将 "EQ-1" 映射为节点ID "EQ:EQ-1"
   → CausalPropagationEngine.propagateImpact(systemDAG, "EQ:EQ-1")
   （实现：`com.zyy.causal.CausalPropagationEngine.propagateImpact(CausalDAG, String)`）

4. BFS遍历，每跳权重×0.8，权重<0.01停止
   → 返回 Map 例如 {EQ:EQ-1=1.0, SYS:EQUIPMENT_MGMT=0.8, SYS:CONSUMABLE_MGMT=0.64, SYS:CONSUMABLE_ALERT=0.512}

5. 返回 CausalCheckResult{impactedNodes=Map{...}, hasHighImpact=true, executionResult="设备删除成功"}
   （实现：`com.zyy.nl.NLService.CausalCheckResult`）
```

**实施例2 — WebSocket实时推送触发场景**

当因果传播计算发现高影响节点（权重≥0.5）时：
- `NLService.CausalCheckResult.hasHighImpact()` 返回 `true`
- Controller层通过 `WebSocketService.pushConsumableAlert(String consumableId, Integer remaining)` 推送告警
- 前端订阅 `/topic/consumable` 接收告警消息，渲染红色操作确认对话框
- 实现类：`com.zyy.websocket.WebSocketService.java`

**实施例3 — 系统初始化时因果图构建**

```
CausalDAGService.init()
  → 在 Spring 容器初始化完成后自动执行（@PostConstruct）
  → 构建系统级因果图（节点：6个，边：6条）
  → Kahn算法拓扑排序验证无环
  → 系统启动后因果传播引擎即可响应预测请求

具体拓扑示例（设备-耗材-告警场景）：

  设备管理节点 (SYS:EQUIPMENT_MGMT)
       │
       ├─[FOREIGN_KEY]─→ 耗材管理节点 (SYS:CONSUMABLE_MGMT)
       │                        │
       │                 [QUALITY_PROPAGATION]
       │                        ↓
       │                 耗材告警节点 (SYS:CONSUMABLE_ALERT)
       │
       ├─[SCHEMA_DEPENDENCY]─→ 操作日志节点 (SYS:OPERATION_LOG)
       │
       └─[FOREIGN_KEY]─→ 巡检记录节点 (IN:DEFAULT)

  说明：上述拓扑仅为本系统的一种具体初始化数据配置示例，
  并不限制保护范围。不同业务场景下的节点类型、边数量和连接关系
  均在本发明保护范围内。

关键类：
  - com.zyy.nl.CausalDAGService  （系统因果图管理，@PostConstruct初始化）
  - com.zyy.causal.CausalDAG     （图数据结构）
  - com.zyy.causal.CausalNode    （节点）
  - com.zyy.causal.CausalEdge    （边，含CausalMechanism枚举）
  - com.zyy.causal.CausalPropagationEngine （传播引擎，decay=0.8）
  - com.zyy.causal.CausalMechanism  （5种机制类型及差异化衰减系数定义）
```